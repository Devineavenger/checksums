name: Release

permissions:
  contents: write

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (optional). If empty, release.sh will determine version from files.'
        required: false
      prerelease:
        description: 'Create prerelease?'
        required: false
        default: 'false'
      draft:
        description: 'Create draft release?'
        required: false
        default: 'false'
      force:
        description: 'Overwrite remote tag if it exists? Set to true to allow destructive update.'
        required: false
        default: 'false'

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      FORCE_TAG_UPDATE: "0"

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl git

      - name: Run release script
        run: |
          set -xeuo pipefail

          # Expand workflow inputs into shell variables
          VERSION="${{ github.event.inputs.version || '' }}"
          PRERELEASE="${{ github.event.inputs.prerelease || 'false' }}"
          DRAFT="${{ github.event.inputs.draft || 'false' }}"
          FORCE_INPUT="${{ github.event.inputs.force || 'false' }}"

          # Build args array
          args=()
          if [ -n "$VERSION" ]; then
            args+=("$VERSION")
          fi
          if [ "$PRERELEASE" = "true" ]; then
            args+=(--prerelease)
          fi
          if [ "$DRAFT" = "true" ]; then
            args+=(--draft)
          fi

          # Set FORCE_TAG_UPDATE only if the workflow input 'force' is true
          if [ "$FORCE_INPUT" = "true" ]; then
            export FORCE_TAG_UPDATE=1
          fi

          bash ./scripts/release.sh "${args[@]}"
